extends layout 

block content
        h1 Send Message
        form(action="/new", method='POST')
            input(
                type='text', name='user', value='', placeholder='Username'
            )
            br
            input(
                type="text", name="text",value='',placeholder="Enter Message"
            )
            br
            input(
                type="submit", value="Submit"
                )

